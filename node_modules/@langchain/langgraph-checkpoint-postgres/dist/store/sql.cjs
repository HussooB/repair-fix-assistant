
//#region src/store/sql.ts
const getStoreTablesWithSchema = (schema) => {
	const tables = [
		"store",
		"store_vectors",
		"store_migrations"
	];
	return tables.reduce((acc, table) => {
		acc[table] = `${schema}.${table}`;
		return acc;
	}, {});
};

//#endregion
exports.getStoreTablesWithSchema = getStoreTablesWithSchema;
//# sourceMappingURL=sql.cjs.map